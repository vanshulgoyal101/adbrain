export default function MetaConnectWaitingPage() {
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-lg items-center px-6 py-12">
      <section className="w-full rounded-lg border border-slate-200 bg-white p-6 shadow-sm">
        <h1 className="text-lg font-semibold text-slate-900">Continue in the Meta window</h1>
        <p className="mt-2 text-sm text-slate-600">
          Complete the guided Meta step in the window that opened. AdBrain will check the saved result when you return.
        </p>
      </section>
    </main>
  );
}